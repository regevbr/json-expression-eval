export declare const functionsTable: {
    user: (user: string, context: {
        userId: string;
    }) => boolean;
    maxCount: (maxCount: number, context: {
        times: number;
    }) => boolean;
};
